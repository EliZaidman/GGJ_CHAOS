using UnityEngine;

public class BenBad : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Hey im ben, and if you are having Global Volume and effect issues its me and i made it in Global Volume in the scene and in its global volume profile");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
